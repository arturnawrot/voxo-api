[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "voxo-api"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.33.0",
]
